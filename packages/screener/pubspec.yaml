name: screener
description: Client for the arbitrage screener server (WebSocket signal stream + REST)
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  decimal: ^3.0.0
  dio: ^5.8.0+1
  network:
    path: ../network
  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  stream_channel: ^2.1.2
